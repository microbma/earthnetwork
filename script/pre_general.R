presence.general <- NULL
for(i in 1:821){
        presence <- edge.presence.sum[combine.1c == names(combine.c)[i]]
        presence.general <- c(presence.general,max(presence))
}

sum(a[2:6])/sum(b[2:6])
